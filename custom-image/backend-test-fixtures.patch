diff --git a/internal/immich/immich_date_pool_test.go b/internal/immich/immich_date_pool_test.go
index fc4bdec..c12ff9e 100644
--- a/internal/immich/immich_date_pool_test.go
+++ b/internal/immich/immich_date_pool_test.go
@@ -5,6 +5,7 @@ import (
 	"encoding/json"
 	"net/http"
 	"net/http/httptest"
+	"path"
 	"sync"
 	"sync/atomic"
 	"testing"
@@ -111,6 +112,11 @@ func dateAssetServer(t *testing.T, calls *atomic.Int32) *httptest.Server {
 func dateAssetServer(t *testing.T, calls *atomic.Int32) *httptest.Server {
 	t.Helper()
 	return httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
+		if r.URL.Path != "/api/search/random" {
+			asset := Asset{ID: path.Base(r.URL.Path), Type: ImageType, ExifInfo: ExifInfo{ExifImageHeight: 1080, ExifImageWidth: 1920}}
+			require.NoError(t, json.NewEncoder(w).Encode(asset))
+			return
+		}
 		call := calls.Add(1)
 		w.Header().Set("Content-Type", "application/json")
 		assets := []Asset{
diff --git a/internal/routes/routes_prefetch_context_test.go b/internal/routes/routes_prefetch_context_test.go
index 5555555..b3c1c9c 100644
--- a/internal/routes/routes_prefetch_context_test.go
+++ b/internal/routes/routes_prefetch_context_test.go
@@ -28,12 +28,28 @@ func TestAssetPreFetchPassesDeadlineToBlockingHTTPTransport(t *testing.T) {
 	require.NoError(t, err)
 	ctx, cancel := context.WithTimeout(context.Background(), 25*time.Millisecond)
 	defer cancel()
-	requestData := &common.RouteRequestData{RequestID: "request-a", DeviceID: "device-a", RequestConfig: config.Config{ImmichURL: "http://immich.test"}}
+	requestConfig := config.Config{
+		ImmichURL:       "http://immich.test",
+		CurationProfile: "deadline",
+		Curation: config.CurationSettings{Profiles: map[string]config.CurationProfile{
+			"deadline": {Sources: []config.CuratedSource{{Type: "date", Value: "last-30", Weight: 1}}},
+		}},
+		Kiosk: config.KioskSettings{FetchedAssetsSize: 1},
+	}
+	requestData := &common.RouteRequestData{RequestID: "request-a", DeviceID: "device-a", RequestConfig: requestConfig}
 	requestCopy := common.ContextCopy{URL: *requestURL, RequestHeader: http.Header{"kiosk-device-id": []string{"device-a"}}, ResponseHeader: make(http.Header)}
 
 	assetPreFetch(ctx, nil, requestData, requestCopy)
-	require.Eventually(t, func() bool { select { case <-cancelled: return true; default: return false } }, time.Second, 5*time.Millisecond)
+	require.Eventually(t, func() bool {
+		select {
+		case <-cancelled:
+			return true
+		default:
+			return false
+		}
+	}, time.Second, 5*time.Millisecond)
 }
 
 type routesRoundTripFunc func(*http.Request) (*http.Response, error)
+
 func (f routesRoundTripFunc) RoundTrip(r *http.Request) (*http.Response, error) { return f(r) }
